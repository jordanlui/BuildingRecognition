function [x,y] = intersect(A)
% Finds intersection between two sets of lines. Inputs are the slope and
% y-intercept data of the line, as well as the upper and lower x bounds
% which signify the domain on which the line exists

end